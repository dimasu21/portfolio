import React, { useState, useEffect } from "react";
import { motion, AnimatePresence } from "framer-motion";

const CursorTrail = () => {
  return null; // Force disabled to fix diagonal lines issue
};

export default CursorTrail;

